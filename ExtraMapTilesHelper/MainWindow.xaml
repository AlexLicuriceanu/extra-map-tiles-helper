<Window x:Class="ExtraMapTilesHelper.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        Title="Extra Map Tiles Helper" Height="768" Width="1366"
        Background="#1E1E1E" 
        WindowStyle="None" 
        ResizeMode="CanResize"
        BorderBrush="#3E3E42" 
        BorderThickness="1"
        AllowDrop="True" Drop="Window_Drop"
        Loaded="Window_Loaded">

    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="32"  
                      ResizeBorderThickness="5" 
                      CornerRadius="0" 
                      GlassFrameThickness="0"/>
    </WindowChrome.WindowChrome>

    <Window.Resources>
        <SolidColorBrush x:Key="MenuBackground" Color="#1B1B1C"/>
        <SolidColorBrush x:Key="MenuBorder" Color="#333337"/>
        <SolidColorBrush x:Key="MenuHover" Color="#3E3E40"/>
        <SolidColorBrush x:Key="MenuText" Color="#F1F1F1"/>
        <SolidColorBrush x:Key="WindowBorder" Color="#3E3E42"/>

        <ControlTemplate x:Key="SubMenuItemTemplate" TargetType="MenuItem">
            <Border x:Name="Border" Background="{StaticResource MenuBackground}" BorderThickness="0" Padding="12,6">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" SharedSizeGroup="Icon"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto" SharedSizeGroup="Shortcut"/>
                    </Grid.ColumnDefinitions>
                    <ContentPresenter Grid.Column="0" ContentSource="Icon" Margin="0,0,10,0" Width="16" Height="16"/>
                    <ContentPresenter Grid.Column="1" ContentSource="Header" RecognizesAccessKey="True" TextElement.Foreground="{StaticResource MenuText}"/>
                </Grid>
            </Border>
            <ControlTemplate.Triggers>
                <Trigger Property="IsHighlighted" Value="True">
                    <Setter TargetName="Border" Property="Background" Value="{StaticResource MenuHover}"/>
                </Trigger>
            </ControlTemplate.Triggers>
        </ControlTemplate>

        <ControlTemplate x:Key="TopLevelHeaderTemplate" TargetType="MenuItem">
            <Grid Background="{TemplateBinding Background}">
                <ContentPresenter ContentSource="Header" RecognizesAccessKey="True" Margin="10,6" TextElement.Foreground="{StaticResource MenuText}"/>

                <Popup x:Name="Popup" Placement="Bottom" IsOpen="{TemplateBinding IsSubmenuOpen}" 
                       AllowsTransparency="False" Focusable="False" PopupAnimation="None"
                       HorizontalOffset="-1" VerticalOffset="0">
                    <Border x:Name="SubmenuBorder" Background="{StaticResource MenuBackground}" 
                            BorderBrush="{StaticResource MenuBorder}" BorderThickness="1" Padding="2">
                        <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Cycle"/>
                    </Border>
                </Popup>
            </Grid>
            <ControlTemplate.Triggers>
                <Trigger Property="IsHighlighted" Value="True">
                    <Setter Property="Background" Value="{StaticResource MenuHover}"/>
                </Trigger>
                <Trigger Property="IsSubmenuOpen" Value="True">
                    <Setter Property="Background" Value="{StaticResource MenuBackground}"/>
                </Trigger>
            </ControlTemplate.Triggers>
        </ControlTemplate>

        <Style TargetType="Separator">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Separator">
                        <Border Background="{StaticResource MenuBorder}" Height="1" Margin="4,2"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="MenuItem">
            <Setter Property="Foreground" Value="{StaticResource MenuText}"/>
            <Setter Property="FontSize" Value="12"/>
            <Style.Triggers>
                <Trigger Property="Role" Value="TopLevelHeader">
                    <Setter Property="Template" Value="{StaticResource TopLevelHeaderTemplate}"/>
                </Trigger>
                <Trigger Property="Role" Value="SubmenuItem">
                    <Setter Property="Template" Value="{StaticResource SubMenuItemTemplate}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="TitleBarButtonStyle" TargetType="Button">
            <Setter Property="Foreground" Value="#CCCCCC"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Width" Value="46"/>
            <Setter Property="WindowChrome.IsHitTestVisibleInChrome" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#3E3E40"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="CloseButtonStyle" TargetType="Button" BasedOn="{StaticResource TitleBarButtonStyle}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#E81123"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="32"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Background="#2D2D30" MouseDown="TitleBar_MouseDown">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Text="Extra Map Tiles Helper" FontSize="12" Margin="15,0,15,0" VerticalAlignment="Center">
                <TextBlock.Style>
                    <Style TargetType="TextBlock">
                        <Setter Property="Foreground" Value="#999999"/>
                        <Setter Property="FontWeight" Value="Bold"/>
                        
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType=Window}, Path=IsActive}" Value="True">
                                <Setter Property="Foreground" Value="#999999"/>
                                <Setter Property="FontWeight" Value="Bold"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>

            <Menu Grid.Column="1" Background="Transparent" VerticalAlignment="Center" WindowChrome.IsHitTestVisibleInChrome="True">
                <MenuItem Header="_File">
                    <MenuItem Header="_Open YTD..." Click="BtnLoadYtd_Click"/>
                    <Separator/>
                    <MenuItem Header="E_xit" Click="BtnClose_Click"/>
                </MenuItem>
                <MenuItem Header="_View">
                    <MenuItem Header="Show Grid"/>
                    <MenuItem Header="Reset View"/>
                </MenuItem>
            </Menu>

            <StackPanel Grid.Column="3" Orientation="Horizontal">
                <Button Content="—" Style="{StaticResource TitleBarButtonStyle}" Click="BtnMinimize_Click"/>
                <Button Content="☐" Style="{StaticResource TitleBarButtonStyle}" Click="BtnMaximize_Click"/>
                <Button Content="✕" Style="{StaticResource CloseButtonStyle}" Click="BtnClose_Click"/>
            </StackPanel>
        </Grid>

        <Grid Grid.Row="1" Background="#1E1E1E">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="280" MinWidth="200" MaxWidth="500"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="280" MinWidth="200" MaxWidth="400"/>
            </Grid.ColumnDefinitions>

            <Grid Grid.Column="0" Background="#252526">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Label Content="Texture Dictionaries" Foreground="#F1F1F1" Background="#2D2D30" Padding="10,6" FontWeight="SemiBold"/>

                <TreeView x:Name="AssetTree" Grid.Row="1" Background="Transparent" BorderThickness="0" Margin="0,5,0,0">
                    <TreeView.Resources>
                        <Style TargetType="TreeViewItem">
                            <Setter Property="Foreground" Value="#DCDCDC"/>
                            <Setter Property="FontSize" Value="13"/>
                            <Setter Property="Padding" Value="2,2"/>
                        </Style>
                    </TreeView.Resources>
                </TreeView>
            </Grid>

            <GridSplitter Grid.Column="1" Width="4" HorizontalAlignment="Center" Background="#3E3E42"/>

            <ScrollViewer x:Name="MapScrollViewer" 
              Grid.Column="2" 
              HorizontalScrollBarVisibility="Auto" 
              VerticalScrollBarVisibility="Auto" 
              Background="#111111"
              PreviewMouseWheel="MapScrollViewer_PreviewMouseWheel">

                <Border BorderBrush="Red" BorderThickness="2" HorizontalAlignment="Center" VerticalAlignment="Center">

                    <Grid x:Name="MapGrid" Width="400" Height="350">
                        <Grid.LayoutTransform>
                            <ScaleTransform x:Name="MapScale" ScaleX="1" ScaleY="1"/>
                        </Grid.LayoutTransform>

                        <Grid.Background>
                            <DrawingBrush TileMode="Tile" Viewport="0,0,50,50" ViewportUnits="Absolute">
                                <DrawingBrush.Drawing>
                                    <GeometryDrawing>
                                        <GeometryDrawing.Geometry>
                                            <GeometryGroup>
                                                <LineGeometry StartPoint="0,0" EndPoint="0,50"/>
                                                <LineGeometry StartPoint="0,0" EndPoint="50,0"/>
                                            </GeometryGroup>
                                        </GeometryDrawing.Geometry>
                                        <GeometryDrawing.Pen>
                                            <Pen Brush="#333" Thickness="1"/>
                                        </GeometryDrawing.Pen>
                                    </GeometryDrawing>
                                </DrawingBrush.Drawing>
                            </DrawingBrush>
                        </Grid.Background>

                        <Canvas x:Name="MapCanvas" 
                    AllowDrop="True" 
                    Drop="MapCanvas_Drop" 
                    Background="Transparent"/>
                    </Grid>
                </Border>
            </ScrollViewer>

            <GridSplitter Grid.Column="3" Width="4" HorizontalAlignment="Center" Background="#3E3E42"/>

            <StackPanel Grid.Column="4" Background="#252526">
                <Label Content="Properties" Foreground="#F1F1F1" Background="#2D2D30" Padding="10,6" FontWeight="SemiBold"/>

                <StackPanel Margin="15">
                    <TextBlock Text="Selected Tile:" Foreground="#999" Margin="0,0,0,5"/>
                    <TextBox x:Name="PropName" Text="None" IsReadOnly="True" Background="#333" Foreground="White" BorderThickness="0" Padding="6"/>

                    <TextBlock Text="Position X" Foreground="#999" Margin="0,15,0,5"/>
                    <TextBox x:Name="PropX" Text="0.0" Background="#3E3E42" Foreground="White" BorderBrush="#555" Padding="4"/>

                    <TextBlock Text="Position Y" Foreground="#999" Margin="0,10,0,5"/>
                    <TextBox x:Name="PropY" Text="0.0" Background="#3E3E42" Foreground="White" BorderBrush="#555" Padding="4"/>

                    <TextBlock Text="Alpha" Foreground="#999" Margin="0,15,0,5"/>
                    <Slider x:Name="PropAlpha" Minimum="0" Maximum="1" Value="1"/>

                    <TextBlock Text="Layer (Z-Index)" Foreground="#999" Margin="0,15,0,5"/>
                    <TextBox x:Name="PropZ" Text="0" Background="#3E3E42" Foreground="White" BorderBrush="#555" Padding="4"/>
                </StackPanel>
            </StackPanel>
        </Grid>
    </Grid>
</Window>